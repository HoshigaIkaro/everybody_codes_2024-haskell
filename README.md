# Running

With Cabal, eg.

```Bash
cabal run cli -- 1
```

or

```Bash
cabal run cli -O2 -- 10 +RTS -c
```

## Input

Format: `./input/q<quest_number>/p<part_number>.txt`

with no zero padding.
